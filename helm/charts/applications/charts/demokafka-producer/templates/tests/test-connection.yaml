apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demokafka-producer.fullname" . }}-test-connection"
  labels:
    {{- include "demokafka-producer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demokafka-producer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
