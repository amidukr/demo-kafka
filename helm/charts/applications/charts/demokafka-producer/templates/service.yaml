apiVersion: v1
kind: Service
metadata:
  name: {{ include "demokafka-producer.fullname" . }}
  labels:
    {{- include "demokafka-producer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demokafka-producer.selectorLabels" . | nindent 4 }}
